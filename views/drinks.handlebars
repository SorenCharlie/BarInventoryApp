   <!DOCTYPE html>
   <html>
   <head>
     <title>Drinks</title>
   </head>
   <body>
     <h1>Drinks</h1>
     <ul>
       {{#each drinks}}
       <li>{{this.name}}</li>
       {{/each}}
     </ul>
   </body>
   </html>
   