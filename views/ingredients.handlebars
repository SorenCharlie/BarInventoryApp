   <!DOCTYPE html>
   <html>
   <head>
     <title>Ingredients</title>
   </head>
   <body>
     <h1>Ingredients</h1>
     <ul>
       {{#each ingredients}}
       <li>{{this.name}} - Quantity: {{this.quantity}} units</li>
       {{/each}}
     </ul>
   </body>
   </html>
   